/**
 * SearchBar — Campo de busca de livros com debounce
 * Implementado no Commit 2 (feat-book-search)
 */

export function SearchBar({ onSearch, isLoading }) {
  // Implementado no Commit 2
  return null
}
